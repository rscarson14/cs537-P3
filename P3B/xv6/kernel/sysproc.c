#include "types.h"
#include "x86.h"
#include "defs.h"
#include "param.h"
#include "mmu.h"
#include "proc.h"
#include "sysfunc.h"

int
sys_fork(void)
{
  return fork();
}

int
sys_exit(void)
{
  exit();
  return 0;  // not reached
}

int
sys_wait(void)
{
  return wait();
}

int
sys_kill(void)
{
  int pid;

  if(argint(0, &pid) < 0)
    return -1;
  return kill(pid);
}

int
sys_getpid(void)
{
  return proc->pid;
}

int
sys_sbrk(void)
{
  int addr;
  int n;
  //int stack_sz;
  //int heap_sz;
  //int n_page;

  if(argint(0, &n) < 0)
    return -1;
  
  /*
  n_page = PGROUNDUP(n); // Round current request to a full page
  stack_sz = proc->s_sz; ; // Grab the size of the stack
  heap_sz = PGROUNDUP(proc->sz); //Grab the size of the heap (rounded up to the nearest page) 
  
  // Check to make sure that the new requested memory will not overrun
  // onto the stack. If so, return error (-1)
  if((heap_sz + n_page) > (stack_sz - PGSIZE)){
    return -1;
  }*/

  addr = proc->sz;
  if(growproc(n) < 0)
    return -1;
  return addr;
}

int
sys_sleep(void)
{
  int n;
  uint ticks0;
  
  if(argint(0, &n) < 0)
    return -1;
  acquire(&tickslock);
  ticks0 = ticks;
  while(ticks - ticks0 < n){
    if(proc->killed){
      release(&tickslock);
      return -1;
    }
    sleep(&ticks, &tickslock);
  }
  release(&tickslock);
  return 0;
}

// return how many clock tick interrupts have occurred
// since boot.
int
sys_uptime(void)
{
  uint xticks;
  
  acquire(&tickslock);
  xticks = ticks;
  release(&tickslock);
  return xticks;
}
